{
	"name": "database",
	"version": "0.0.0",
	"scripts": {
		"db:migrate": "npx prisma migrate dev --name dev_migrations",
		"db:generate": "npx primsa generate"
	},
	"devDependencies": {
		"prisma": "^5.6.0"
	},
	"dependencies": {
		"@prisma/client": "^5.6.0",
		"dotenv-cli": "^7.3.0"
	}
}

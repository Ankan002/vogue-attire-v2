export const protected_route_matchers = [];

export const protected_routes = ["/"];

export const unprotected_routes = ["/login"];

export const protected_api_route_matchers = [];

export const protected_api_routes = ["/api/auth/logout"];

export const unprotected_api_routes = ["/api/auth/login"];
